# Unified Application Checkpoint

The original baseline checkpoint has been superseded by [UNIFIED_REMEDIATION_CHECKPOINT.md](UNIFIED_REMEDIATION_CHECKPOINT.md).

The remediation removes contribution-weight and global Final KPI template configuration from the new workflow, makes `content_urls` the primary read-only source, and introduces per-Member x Month targets/configuration with isolated Preview verification. Legacy schema objects remain only to preserve history and compatibility; they are not required by the new workflow.
