import { getServerSession } from "next-auth";import { redirect } from "next/navigation";
import { authOptions } from "../../../lib/auth";import { DataTableContainer,MetricCard,Shell } from "../../../components/ui";import { KpiCloseControls,KpiTemplateForm } from "../../../components/unified-workflows";import { listMonthlyKpiAudit } from "../../../lib/repositories/monthly-kpi";import { listKpiTemplates } from "../../../lib/repositories/kpi-template";
export const dynamic="force-dynamic";const pct=(v:any)=>v===null||v===undefined?"N/A":`${Number(v).toFixed(1)}%`;
export default async function KpiClose(props:{searchParams?: Promise<{month?:string}>}) {
  const searchParams = await props.searchParams;
  const session=await getServerSession(authOptions);if(!session?.user?.email||!session.user.isAdmin)redirect("/");const month=searchParams?.month??new Date().toISOString().slice(0,7);const[audit,templates]=await Promise.all([listMonthlyKpiAudit(month),listKpiTemplates()]);const members=[...new Set([...audit.targets.map((r:any)=>r.member_name),...audit.components.map((r:any)=>r.member_name)])];return <Shell email={session.user.email} isAdmin><div className="space-y-6"><header><p className="text-sm font-semibold uppercase text-blue-700">Admin workflow 6/6</p><h2 className="text-3xl font-bold">KPI Close — {month}</h2><p className="mt-2 text-slate-600">Preview, enter Social + Video or N/A, finalize, lock, export audit, and reopen as a new immutable version.</p></header><KpiTemplateForm/><div className="grid gap-4 md:grid-cols-3"><MetricCard label="Templates" value={templates.length}/><MetricCard label="Members" value={members.length}/><MetricCard label="Locked" value={audit.results.filter((r:any)=>r.status==="locked").length}/></div><KpiCloseControls month={month} members={members} lockedResults={audit.results.filter((r:any)=>r.status==="locked").map((r:any)=>({id:String(r.id),memberName:r.member_name}))}/><DataTableContainer><table className="min-w-full text-sm"><thead className="bg-slate-100 text-left"><tr><th className="p-3">Member</th><th>Version</th><th>Raw</th><th>Payable</th><th>Coverage</th><th>Status</th><th>Payout</th><th>Locked</th></tr></thead><tbody>{audit.results.map((row:any)=><tr className="border-t" key={row.id}><td className="p-3">{row.member_name}</td><td>{row.version}</td><td>{pct(row.raw_pct)}</td><td>{pct(row.payable_pct)}</td><td>{pct(row.coverage_pct)}</td><td>{row.status}</td><td>{row.payout_vnd?.toLocaleString?.()??"N/A"}</td><td>{row.locked_at?new Date(row.locked_at).toLocaleString():"—"}</td></tr>)}</tbody></table></DataTableContainer><a className="inline-flex rounded-lg border px-4 py-2 font-semibold text-blue-700" href={`/api/admin/kpi-month/${month}/audit`}>Export audit JSON</a></div></Shell>
}
