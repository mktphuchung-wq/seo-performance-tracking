import { getServerSession } from "next-auth";import { redirect } from "next/navigation";
import { authOptions } from "../../../lib/auth";import { DataTableContainer,Shell } from "../../../components/ui";
import { ContributionWeightsForm,ProjectSettingsForm } from "../../../components/unified-workflows";import { listUnifiedProjectSettings } from "../../../lib/repositories/project-settings";
export const dynamic="force-dynamic";
export default async function ProjectsPage(){const session=await getServerSession(authOptions);if(!session?.user?.email||!session.user.isAdmin)redirect("/");const data=await listUnifiedProjectSettings();return <Shell email={session.user.email} isAdmin><div className="space-y-6"><header><p className="text-sm font-semibold uppercase text-blue-700">Admin workflow 2/6</p><h2 className="text-3xl font-bold">Project Settings</h2><p className="mt-2 text-slate-600">A single effective-dated configuration for domain ownership, lifecycle, GSC/KPI readiness, 3M/6M/All Time weights, and member contributions.</p></header><ProjectSettingsForm/><ContributionWeightsForm members={data.members.map((row:any)=>row.canonical_name)} projects={data.projects.map((row:any)=>row.canonical_name)}/><DataTableContainer><table className="min-w-full text-sm"><thead className="bg-slate-100 text-left"><tr><th className="p-3">Project</th><th>Domain</th><th>Lifecycle</th><th>GSC</th><th>KPI</th><th>3M / 6M / All</th><th>Version</th><th>Status</th></tr></thead><tbody>{data.projects.map((row:any)=><tr className="border-t" key={row.id}><td className="p-3">{row.canonical_name}</td><td>{row.canonical_domain??"Not configured"}</td><td>{row.lifecycle??"Not configured"}</td><td>{row.gsc_ready?"Ready":"Not ready"}</td><td>{row.kpi_ready?"Ready":"Not ready"}</td><td>{row.performance_weight_3m_pct??"—"} / {row.performance_weight_6m_pct??"—"} / {row.performance_weight_all_time_pct??"—"}</td><td>{row.version??"—"}</td><td>{row.status??"—"}</td></tr>)}</tbody></table></DataTableContainer></div></Shell>}
