import { getServerSession } from "next-auth";import { redirect } from "next/navigation";
import { authOptions } from "../../lib/auth";import { DataTableContainer,MetricCard,Shell } from "../../components/ui";import { resolveMemberNameByEmail } from "../../lib/member-identity";import { listMonthlyKpiAudit } from "../../lib/repositories/monthly-kpi";
export const dynamic="force-dynamic";const pct=(v:any)=>v===null||v===undefined?"N/A":`${Number(v).toFixed(1)}%`;
export default async function MyKpi(props:{searchParams?: Promise<{month?:string}>}) {
  const searchParams = await props.searchParams;
  const session=await getServerSession(authOptions);if(!session?.user?.email)redirect("/");if(session.user.isAdmin)redirect("/admin/kpi-close");const memberName=await resolveMemberNameByEmail(session.user.email);if(!memberName)redirect("/");const month=searchParams?.month??new Date().toISOString().slice(0,7);const audit=await listMonthlyKpiAudit(month,memberName);const final=audit.results[0];return <Shell email={session.user.email}><div className="space-y-6"><header><p className="text-sm font-semibold uppercase text-blue-700">Member workspace</p><h2 className="text-3xl font-bold">My KPI — {month}</h2><p className="mt-2 text-slate-600">Component breakdown and immutable Final KPI snapshot. N/A is never displayed as zero.</p></header><div className="grid gap-4 md:grid-cols-3"><MetricCard label="Final KPI" value={pct(final?.payable_pct)}/><MetricCard label="Coverage" value={pct(final?.coverage_pct)}/><MetricCard label="Status" value={final?.status??"Draft"}/></div><DataTableContainer><table className="min-w-full text-sm"><thead className="bg-slate-100 text-left"><tr><th className="p-3">Component</th><th>Raw</th><th>Payable</th><th>Coverage</th><th>Status</th><th>Reason</th><th>Rule</th></tr></thead><tbody>{audit.components.map((row:any)=><tr className="border-t" key={row.id}><td className="p-3">{row.component_key.replace(/_/g," ")}</td><td>{pct(row.raw_pct)}</td><td>{row.is_not_applicable?"N/A":pct(row.payable_pct)}</td><td>{pct(row.coverage_pct)}</td><td>{row.status}</td><td>{row.na_reason??row.reason??row.override_reason??"—"}</td><td>{row.rule_version}</td></tr>)}</tbody></table></DataTableContainer></div></Shell>;
}
